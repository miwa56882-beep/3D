{
  "name": "3d",
  "version": "1.0.0",
  "description": "提供された `1F.bmp` から `4F.bmp` をもとに、WebGL で操作できる 3D 校内案内図を構築しました。ブラウザ側の 3D 描画には OSS の `Three.js` (MIT License) を使っています。",
  "main": "build-single-html.mjs",
  "scripts": {
    "build:single": "node ./build-single-html.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "esbuild": "^0.28.0",
    "three": "^0.176.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
